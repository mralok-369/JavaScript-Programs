console.log("Math objects in javaScript");
let x = 3;
let y = 7;
let z;
z = x + y;
z = x - y;
z = x * y;
z = x / y;

// Exploring the math Object
z = Math;
z = Math.PI;
z = Math.E;
z = Math.round(5.4);  // rounding off the number
z = Math.ceil(5.2);   // increase the number near high int
z = Math.floor(5.9);  // decrease the num near low int
z = Math.floor(-5.1);
z = Math.abs(-5);  // find absolute value of given number
z = Math.sqrt(9);  // find square root of the number
z = Math.pow(5, 3);  // find power of any number
z = Math.max(-5, 52, 63, 41, 15); // find max number
z = Math.min(-5, 52, 63, 41, 15);  // find in number
z = Math.random(); // num b/w 0 to 1 default
z = 10 + (20 - 10) * Math.random(); // b/w 10 to 20
z = 20 * Math.random();  // b/w 0 to 20
z = Math.ceil(20 * Math.random());  // b/w 0 to 20 int val
console.log(z);